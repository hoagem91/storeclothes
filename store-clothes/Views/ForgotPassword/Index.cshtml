@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quên mật khẩu - LITA</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="~/css/login.css" />
</head>
<body>

    <div class="login-container">
        <!-- Hình ảnh bên trái -->
        <div class="image-section"></div>

        <!-- Form quên mật khẩu bên phải -->
        <div class="form-section">
            <div class="form-container">
                <h3 class="text-center">Quên mật khẩu?</h3>
                <p class="text-center text-muted">Nhập email của bạn và chúng tôi sẽ gửi cho bạn một liên kết để đặt lại mật khẩu.</p>

                <form method="post" action="@Url.Action("ForgotPassword", "Account")">
                    <input type="email" class="input-field" name="email" placeholder="Nhập email của bạn" required>
                    <button type="submit" class="btn-login">Gửi liên kết đặt lại</button>
                </form>

                <div class="text-center mt-3">
                    <a href="@Url.Action("Index", "Login" )">Quay lại đăng nhập</a>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
